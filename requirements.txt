# Vercel(@vercel/python) はリポジトリルートの requirements.txt を読む。
# 中身は backend/requirements.txt と同じに保つこと（uvicorn を除く / uvicorn は本番Vercelでは不要）。
# 同期確認: `make check-requirements`（差分があれば失敗する）。
# Python は 3.12 に統一（.python-version / backend/Dockerfile / Vercel プロジェクト設定）。
fastapi==0.115.6
sqlalchemy==2.0.36
pydantic[email]==2.13.4
python-multipart==0.0.20
psycopg[binary]==3.3.4
bcrypt==5.0.0
pyjwt==2.10.1
python-dotenv==1.0.1
