# ルートの requirements.txt と uvicorn 以外は同期させること（`make check-requirements`）。
# Python は 3.12 に統一（.python-version / Dockerfile / Vercel プロジェクト設定）。
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic[email]==2.13.4
python-multipart==0.0.20
psycopg[binary]==3.3.4
bcrypt==5.0.0
pyjwt==2.10.1
python-dotenv==1.0.1
